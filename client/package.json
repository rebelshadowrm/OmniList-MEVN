{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run",
    "verify:phase2": "npm run typecheck && npm run test && npm run build",
    "verify:phase4": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "@types/node": "^25.6.0",
    "axios": "^1.20.0",
    "pinia": "^2.1.7",
    "typescript": "^6.0.3",
    "vue": "^3.2.25",
    "vue-router": "^4.0.13",
    "vue-tsc": "^3.2.7"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
